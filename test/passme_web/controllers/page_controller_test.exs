defmodule PassmeWeb.PageControllerTest do
  use PassmeWeb.ConnCase

  test "GET /", %{conn: _conn} do
    # conn = get(conn, "/")
    # assert html_response(conn, 200) =~ "Welcome to Phoenix!"
  end
end
